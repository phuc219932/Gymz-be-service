<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
             <!-- <pattern>%d{yyyy-MM-dd HH:mm:ss} - [%thread] - %highlight([%level]) %logger{36} - User: %X{userId} - %msg%n</pattern> -->
             <pattern>%d{yyyy-MM-dd HH:mm:ss} - [%thread] - %highlight([%level]) %logger{36} - User: %X{userId} - Username: %X{username} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - [%thread] - %highlight([%level]) %logger{36} - UserId: %X{userId} - Username: %X{username} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>localhost:5044</destination> <!-- Send logs to Logstash -->
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>

    
    <root level="INFO">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE" />
    <!-- <appender-ref ref="LOGSTASH" /> -->

    </root>

</configuration>
